version: 0.2

phases:
  pre_build:
    commands:
      - echo "Checking Docker..."
      - docker --version

  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t myapp .
      - echo "Running container..."
      - docker run myapp

artifacts:
  files:
    - '**/*'
